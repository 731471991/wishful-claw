/**
 * Open a session in a detached window.
 * Placeholder — returns without action.
 * TODO (后续迭代): Implement detached session window using Electron's BrowserWindow.
 */
export function openSessionOrFocusDetached(sessionId: string): void {
  // Placeholder: for now, just switch the active session in-place.
  // Later iterations will open a separate window.
  void sessionId
}

export function openDetachedSessionWindow(sessionId: string): void {
  void sessionId
}
