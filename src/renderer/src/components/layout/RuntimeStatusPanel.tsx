/**
 * Runtime Status Panel — placeholder.
 * TODO (迭代四): Show agent runtime status, token usage, model info.
 */
export function RuntimeStatusPanel(): React.JSX.Element {
  return (
    <div className="flex h-full items-center justify-center p-4 text-xs text-muted-foreground">
      Runtime status — coming soon
    </div>
  )
}
