// Stub: pet-skin-store (to be filled when pet features are migrated)

export function getPetsDir(): string {
  return ""
}

export const usePetSkinStoreStore = {
  getState: () => ({}),
  subscribe: () => () => {},
}
